meta {
  name: Próximas Sessões
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/lms/sessions/upcoming?page=1&limit=20
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Paginação", function() {
    expect(res.body).to.have.property("totalPages");
  });
}
