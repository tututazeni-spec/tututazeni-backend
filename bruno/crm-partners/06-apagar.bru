meta {
  name: Apagar Parceiro
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/crm/partners/{{partnerId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Mensagem sucesso", function() {
    expect(res.body.message).to.contain("sucesso");
  });
}
