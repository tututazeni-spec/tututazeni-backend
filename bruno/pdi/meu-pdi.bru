meta {
  name: Meu PDI (legacyPdi)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/development-plans/my
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status deve ser 200 ou 403", function() {
    expect([200, 403]).to.include(res.status);
  });
}
